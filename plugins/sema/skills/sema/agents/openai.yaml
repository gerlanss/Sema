interface:
  display_name: "Sema"
  short_description: "Separate Sema install, repair, and adoption"
  default_prompt: "Use $sema to classify global install or update, distribution repair, informational use, and explicit workspace adoption before running the correct isolated flow."
